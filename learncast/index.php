<?php

$business = [
    'name' => 'Laracasts',
    'cost' => 15,
    'categories' => ["Testing", "PHP", "JavaScript"]
];

// Se necesita un punto y coma al final de cada sentencia.
function register($user) {
    // Create the user record in the db.
    // Log them in.
    // Send a welcome email.
}

require "index.view.php";
